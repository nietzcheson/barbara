{% if app.user.path !="" %}
    <img src="{{ asset('uploads/profiles/') }}{{ app.user.path }}" class="img-profile" />
{% else %}
    <img src="{{ asset('img/avatar.png') }}" class="img-profile" />
{% endif %}
