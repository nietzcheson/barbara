<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand"><span class="title">
        <img src="{{ asset('img/isotipo.png') }}" style="max-width:100px; margin-top: -12px;" />  Bárbara Castro | </span><i class="subtitle">Elearning</i>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav navbar-right">
          <li><a href="{{ path('moocsy_admin_courses') }}">Admin</a></li>
            <a class="navbar-brand" rel="home" href="" title="">
                {% include 'AppBundle::includes/img-profile-navbar.html.twig' %}
            </a>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              {% if app.user.name == "" %} {{ app.user.username }} {% else %} {{ app.user.name }} {% endif %}<span class="caret"></span></a>
          <ul class="dropdown-menu">
            {% include 'AppBundle::includes/navbar-li.html.twig' %}
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
